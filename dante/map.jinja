{% set dante = salt['grains.filter_by']({
    'Debian': {
        'server': {
          'pkg': 'dante-server',
          'service': 'danted',
          'config': '/etc/danted.conf',
        }
    },
}, merge=salt['pillar.get']('dante:lookup')) %}

